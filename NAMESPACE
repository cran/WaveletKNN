
export(WaveletKNN)
import("Metrics",except=c(precision,recall,accuracy))
import(caret)
import(caretForecast)
import("dplyr", except=c(count,filter,lag))
import(stats)
import(tseries)
import(wavelets)
